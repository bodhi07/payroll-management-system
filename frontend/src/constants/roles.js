/**
 * System Roles Constant
 *
 * Defines the user authorization roles supported by the Spring Boot backend
 * and enforced by the React frontend via RoleBasedRoute.
 */
export const ROLES = {
  ADMIN: 'ADMIN',
  HR: 'HR',
  EMPLOYEE: 'EMPLOYEE',
};
